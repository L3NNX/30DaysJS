// Import the person object from the module
const person = require('./person');

// Access the properties and methods of the person object
console.log(person.name); 
console.log(person.age); 
person.greet(); 
